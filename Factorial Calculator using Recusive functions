#include<iostream>
using namespace std;

 void fact ( int x);


int main ()
{
    int number, factorial=1;   // store number that we wish to calculate , variable , (1) is what will initalize the

    // factorial stores the values we calculate



    cout<< " enter the number "<< endl;
    cin>> number;

    if (number <0 )
        cout << " cant find number for negative number ";
    else if (number <=1)
        cout<< number << "!=" <<factorial;
    else {
        for (int counter= number;counter >=2; counter -- ){
            factorial = factorial * counter ;
        }
        cout<< number << "!=" <<factorial;
    }

//        return to 0;



}
